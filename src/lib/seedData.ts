import { UserProfile, Negotiation, Offer, ChatMessage, NegotiationDocument, Agreement } from '../types';

// Mock and demo seed data removed. Application operates strictly on live Firebase data.
export const SEED_USERS: UserProfile[] = [];
export const SEED_NEGOTIATIONS: Negotiation[] = [];
export const SEED_OFFERS: Record<string, Offer[]> = {};
export const SEED_MESSAGES: Record<string, ChatMessage[]> = {};
export const SEED_DOCUMENTS: Record<string, NegotiationDocument[]> = {};
export const SEED_AGREEMENTS: Record<string, Agreement> = {};
